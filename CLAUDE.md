# CLAUDE.md

## 프로젝트

**MAP OS (MapDecision)** — 사용자의 생각과 고민을 대화로 끌어내어, 글이 아니라 **시각화된 지도(MAP)**로 보여주는 의사결정 시각화 AI 플랫폼입니다.

> 정답을 대신 주는 AI가 아니라, 내 생각이 보이게 만드는 AI.

## 현재 소스 오브 트루스 (중요)

- 실제로 운영 중인 서비스 코드는 **`src/map-decision-v1`** 하나뿐입니다. 새 기능/수정은 반드시 이 폴더 안에서 진행하세요.
- 제품 정의는 `README.md`가 아니라 **`docs/MAP_CONSTITUTION.md`**, **`docs/MAP_DESIGN_SYSTEM.md`**를 기준으로 삼으세요. `README.md`는 현재 구현과 맞지 않는 내용을 담고 있어 신뢰할 수 없습니다 (정리 예정). 코드를 만지기 전에 이 두 문서를 먼저 읽으세요. (`AGENTS.md`에도 유사한 체크리스트가 있으니 같이 참고하세요.)
- `README.md`가 설명하는 `map-os` 파이프라인(Question Engine → ... → Share Renderer)은 실제 구현과 맞지 않습니다. 지금 당장 삭제하거나 수정하지 말고, 이 문서를 통해 "신뢰하지 말 것"으로만 인지하세요.

## 죽은 코드 (건드리지 말 것)

다음 폴더는 어디서도 사용되지 않는 고립된 실험 코드입니다. 참고하거나 재사용하지 마세요:

- `src/map-os`
- `src/conversation-engine`
- `src/discovery-engine`
- `src/story-engine`
- `src/map-engine` (`IdealTypeResult.tsx`를 통해서만 "연결"되어 있으나 렌더링되지 않음)

지금은 삭제하지 않습니다. 삭제는 별도 정리 PR에서 오너 승인 후 진행합니다.

## 개발 원칙

1. **코드보다 UX/시각화가 먼저다.** 기능이 동작하는 것만으로는 완료가 아닙니다 — 실제 화면에서 "보기 좋고, 이해되고, 스크린샷 하고 싶은" 결과가 나와야 완료입니다.
2. **작게 개발하고, 자주 배포한다.** 큰 기능을 한 번에 만들지 말고, 눈에 보이는 작은 단위로 쪼개서 자주 내보냅니다.
3. **항상 Feature Branch + PR로 작업한다. main에 직접 커밋하지 않습니다.** 아무리 작은 수정이라도 브랜치를 새로 파고 PR을 엽니다.
   - **정정(2026-08-10 조사): "Quality Gate 통과 시 자동으로 main에 머지된다"는 설계 의도일 뿐, 실제로는 작동하지 않습니다.** PR을 항상 Draft로 먼저 여는 이 저장소의 관행과 자동 머지 워크플로의 트리거 설정이 서로 어긋나 있어(자세한 원인은 `docs/CURRENT_STATE.md`의 "CI / 브랜치 보호 · 머지 방식" 참고), 실제로는 **오너가 GitHub에서 Ready 전환 후 직접 "Merge pull request" 버튼을 눌러야 main에 반영됩니다.** 이 문제는 의도적으로 고치지 않기로 했습니다 — 지금의 수동 머지가 결과적으로 마지막 검토 지점 역할도 하기 때문입니다.
   - **"완료 보고 전 머지 금지" 요청이 필요한 진짜 이유(2026-08-10 정정)는 자동 머지가 아닙니다.** 자동 머지는 애초에 Draft PR을 못 건드리니 이 요청과 직접 부딪힐 일이 없습니다. 진짜 이유는 **작업이 아직 끝나지 않은 브랜치가 먼저 머지되면, 그 뒤에 이어지는 커밋이 어떤 PR에도 담기지 못하고 고립되기 때문**입니다 — 2026-08-09 PR #231에서 실제로 이렇게 커밋 2개가 유실돼 PR #233으로 복구했습니다(자세한 경위는 아래 "머지된 브랜치 재사용 금지" 참고).

## 머지된 브랜치 재사용 금지

PR이 머지된 뒤에는 그 브랜치에 새 커밋을 이어붙이지 않습니다. 작업이 남았으면 최신 main에서 새 브랜치를 만들고 새 PR을 엽니다.

작업 시작 전 항상 확인합니다:
- 지금 브랜치의 PR이 이미 머지됐는가
- 머지됐다면 최신 main에서 브랜치를 다시 시작한다

**이유**: 머지되면 그 브랜치를 지켜보는 PR이 사라집니다. 이후 커밋은 어떤 검사도 받지 않고 어떤 PR에도 담기지 않은 채 고립됩니다. 2026-08-09 PR #231에서 커밋 2개가 이렇게 유실돼 #233으로 복구했습니다 — `feat/friendship-card-status-label` 브랜치에 4개 커밋이 순서대로 푸시됐는데, PR #231은 처음 2개만으로 머지됐고(08:03), 나머지 2개는 각각 머지 11분 뒤·2시간 13분 뒤에 같은 브랜치에 이어붙여져 어느 PR에도 속하지 못한 채 남았습니다. (이 사고는 자동 머지와 무관합니다 — PR #231도 오너가 직접 머지했습니다. 자세한 조사 경위는 `docs/CURRENT_STATE.md`의 "머지된 브랜치 재사용 금지" 참고.)

## PR 작성 규칙

모든 PR 설명에는 다음을 반드시 포함합니다:

- **목적**: 이 PR이 왜 필요한지 (한두 문장)
- **변경 파일**: 무엇을 바꿨는지 파일 단위로
- **스크린샷**: 화면이 바뀌면 반드시 전/후 스크린샷 첨부 (모바일/데스크톱 둘 다 권장)
- **테스트 결과**: `typecheck`, `harness:check`, `design:check`, `build` 실행 결과 (통과/실패)
- **배포 상태**: Vercel Preview 링크와 확인 여부
- **리스크**: 이 변경으로 깨질 수 있는 부분, 알려진 한계

## 조사 전용 작업

"코드를 수정하지 말고 조사만 하라"는 지시를 받은 턴에서는 저장소 안에 어떤 변경도 남기지 않습니다.

- 임시 스크립트·캡처 파일은 저장소 밖(예: `/tmp`)에 만듭니다.
- 부득이하게 저장소 안에 만들었다면 턴이 끝나기 전에 반드시 삭제합니다.
- 조사가 끝난 시점에 `git status`가 완전히 깨끗해야 합니다.

이유: 세션 인프라의 Stop hook(`/root/.claude/stop-hook-git-check.sh`)이 추적·미추적 변경이나 미푸시 커밋을 감지하면 턴 종료를 막고 커밋·푸시를 요구합니다. 이 훅은 저장소 밖 설정이라 수정할 수 없고, 현재 턴의 지시 내용을 알지 못합니다. 따라서 조사 턴에서 변경을 남기면 오너의 "커밋 금지" 지시와 훅이 충돌하게 됩니다.

## "반영하자"의 의미

오너가 "반영하자"라고 말하면 **이 변경을 승인한다**는 뜻입니다. 하지만 이것이 **품질 게이트(typecheck·harness check·design check·build)를 생략해도 된다는 뜻은 아닙니다.** 승인은 "이 방향이 맞다"는 결정이고, 품질 검증은 별개로 항상 거칩니다.

**검사 통과가 곧 프로덕션 반영으로 이어지는 구조로 설계돼 있었지만, 지금은 자동 머지 단계가 실제로 작동하지 않아 오너의 수동 머지가 그 사이에 껴 있습니다(2026-08-10 조사, 위 "개발 원칙" 3번 참고).** main은 Vercel Production(`mapdecision.com`)에 바로 배포되므로, PR을 열기 전이나 검사가 통과되기 전에 화면을 직접 확인하는 것은 여전히 중요한 사람 검증 기회입니다 — 다만 지금은 그것이 "유일한" 기회는 아니고, Ready 전환 후 오너가 직접 머지 버튼을 누르는 순간이 사실상 마지막 검토 지점입니다. 자동 검사는 타입 오류·빌드 실패·금지된 raw 색상값만 잡아내며 UX/시각 품질은 걸러주지 않으므로, "코드보다 UX/시각화가 먼저다" 원칙에 따라 PR을 열기 전에 반드시 화면을 직접 확인하세요.

## 기술 설명 방식

이 레포의 오너는 비개발자입니다. PR 설명, 커밋 메시지, 대화에서 기술 개념을 언급할 때는:

- 전문 용어를 그대로 쓰지 말고 쉬운 한국어로 풀어서 설명합니다. (예: "하이드레이션 에러" → "화면이 처음 그려질 때와 나중에 그려질 때 내용이 달라서 깜빡이거나 깨지는 문제")
- "왜 필요한지 / 사용자 입장에서 뭐가 바뀌는지"를 기술 구현보다 먼저 설명합니다.
- 코드 구조를 설명할 때도 비유나 화면 흐름 중심으로 풀어줍니다.
